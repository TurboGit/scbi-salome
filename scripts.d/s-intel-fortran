#  Only used on Windows for now

SCBI_W_IFOR=$(cygpath -w -s "$SCBI_WINDOWS_IFOR")

with-variant native s-windows-kit \
  --env 'add-to-var PATH    "$SCBI_W_IFOR/bin"' \
        'add-to-var LIB     "$SCBI_W_IFOR/lib"'
